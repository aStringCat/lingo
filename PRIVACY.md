# 隐私说明

Lingo 不收集、出售或上传遥测数据，也没有开发者自有服务器。

当且仅当用户点击“翻译当前网页”或使用快捷键时，扩展才会临时注入当前标签页并读取正文式文本块。打开网页或仅打开扩展面板不会注入脚本或读取页面。Chrome 的内置 Translator API 可用时，翻译在浏览器管理的本地语言模型中完成。

在线翻译兜底默认关闭。只有用户在扩展面板中主动开启并授予 `api.mymemory.translated.net` 访问权限后，本地翻译不可用时才会把选中的正文发送给该服务；关闭开关会撤销这项权限。密码框、输入框、可编辑区域、代码块和表单内容不会被读取或发送。

目标语言与显示方式保存在浏览器提供的同步存储中。译文只保存在当前页面 DOM 和扩展后台的短期内存缓存中；关闭页面或后台进程结束后不会持久保留。

使用在线回退服务时，翻译文本受 MyMemory 对应服务条款与隐私政策约束。
